spring.application.name=polaris

jwt.secret=${JWT_SECRET:my-very-jwt-secret}
jwt.access-token-expiration-seconds=${ACCESS_TOKEN_EXPIRATION_SECONDS:900}

refresh-token.expiration-days=${REFRESH_TOKEN_EXPIRATION_DAYS:7}

spring.jpa.hibernate.ddl-auto=validate
